function printBanner(centertext) {
    let row = '*';
    let textLength = centertext.length;
    console.log(`${row.repeat(textLength)}`)
    console.log(`${centertext}`) 
    console.log(`${row.repeat(textLength)}`)

    
}
    printBanner('* Welcome to Digital Crafts *');


